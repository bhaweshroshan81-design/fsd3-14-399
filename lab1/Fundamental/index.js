function sayHello (name) {
    console.log(`hello ${name}`);
    }
    const sayHi = (name) => {
        console.log(`Hi ${name}`);  // to concatinate variable in also use "$" sign to add variable
    }
    const findSum = (a, b) => {
        return a + b;
    };
            const sum = (a, b) => a + b;
            sayHello("Bhawesh Raushan");
            sayHi("Adarsh");