# http module

hyper text transfer protocol
html - hyper text markup language
css - cascade style sheet
Reandering - give the content of html on browser
npm - node package manager

what is API?
any PI can be of four types....

1. GET ---- READ 
2. POST ---- CREATE
3. PUT/ patch --4
4. DELET

API can be responsive by server with status code and json data. api generally starts with api/version.

for example  - /api/ v1/products
            - .api/ 


By default browser can check only get requst. TO check other three request types like post, put ,patch , and delete